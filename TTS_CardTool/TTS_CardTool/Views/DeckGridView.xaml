<UserControl x:Class="TTS_CardTool.Views.DeckGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TTS_CardTool.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<DataGrid ItemsSource="{Binding CardList}"
				  CanUserAddRows="True"
				  CanUserDeleteRows="True"
				  CanUserResizeRows="False"
				  ColumnWidth="*"
				  AutoGenerateColumns="False">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Card title" Binding="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
				<DataGridTextColumn Header="Description" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="4*"/>
				<DataGridTextColumn Header="Amount in deck" Binding="{Binding Count, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
			</DataGrid.Columns>
		</DataGrid>
    </Grid>
</UserControl>
